domainBase: "localtest.me"
vi1:
  batch:
    config:
      enabled: false
  iframe1:
    config:
      enabled: false
  instancesettings:
    config:
      enabled: false
  etcspringboot:
    config:
      enabled: false
  commercepaymentintegration:
    config:
      enabled: false
  commerceshippingengine:
    config:
      enabled: false
vi2:
  batch:
    config:
      enabled: false
  iframe1:
    config:
      enabled: false
  instancesettings:
    config:
      enabled: false
  etcspringboot:
    config:
      enabled: false
  commercepaymentintegration:
    config:
      enabled: false
  commerceshippingengine:
    config:
      enabled: false
vi3:
  batch:
    config:
      enabled: true
    workload:
      enabled: true
  iframe1:
    config:
      enabled: false
  instancesettings:
    config:
      enabled: false
  etcspringboot:
    config:
      enabled: true
  commercepaymentintegration:
    config:
      enabled: false
  commerceshippingengine:
    config:
      enabled: false
